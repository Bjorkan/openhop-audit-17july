# POSSIBLE-ENHANCEMENT-011: Possible enhancement — centralize companion delivery outcomes and deduplication commit
# Audit date: 2026-07-17
# Classification: Possible enhancement
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/repeater/packet_router.py
+++ b/repeater/packet_router.py
@@
+@dataclass(frozen=True)
+class CompanionDeliveryResult:
+    attempted: int
+    authenticated: int
+    failed: int
+
+    @property
+    def delivered(self) -> bool:
+        return self.authenticated > 0
+
+async def _deliver_to_companions(..., dedupe: bool = False) -> CompanionDeliveryResult:
+    ...
