# BUG-022: Update-channel persistence failure is hidden behind a success response
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/repeater/web/update_endpoints.py
+++ b/repeater/web/update_endpoints.py
@@
-    def _save_channel(self, channel: str) -> None:
+    def _save_channel(self, channel: str) -> bool:
         try:
-            ...
+            atomic_write_text(_CHANNELS_FILE, channel)
+            return True
         except OSError as exc:
             logger.warning(...)
+            return False
@@
-        _state.set_channel(channel)
+        if not _state.set_channel(channel):
+            return self._err("Could not persist update channel", 500)
