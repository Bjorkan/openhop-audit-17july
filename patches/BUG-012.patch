# BUG-012: Quick mode and duty controls are volatile while the terminal labels them persisted
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

--- a/repeater/web/api_endpoints.py
+++ b/repeater/web/api_endpoints.py
@@
-            self.config["repeater"]["mode"] = new_mode
-            return {"success": True, "mode": new_mode}
+            result = self.config_manager.update_and_save(
+                {"repeater": {"mode": new_mode}},
+                live_update=True,
+                live_update_sections=["repeater"],
+            )
+            if not result.get("saved"):
+                return self._error(result.get("error", "Failed to save mode"))
+            return self._success({"mode": new_mode, "persisted": True,
+                                  "live_update": result.get("live_updated", False)})
