# BUG-005: SX1262 live radio updates omit transmit power
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

--- a/repeater/config_manager.py
+++ b/repeater/config_manager.py
@@
                 if not applied:
                     return False
+                current_tx_power = getattr(radio, "tx_power", None)
+                if current_tx_power != radio_cfg["tx_power"]:
+                    if not hasattr(radio, "set_tx_power"):
+                        return False
+                    if not radio.set_tx_power(radio_cfg["tx_power"]):
+                        return False
