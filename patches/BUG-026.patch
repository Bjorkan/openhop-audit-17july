# BUG-026: Offline companion messages are dequeued before the response frame is accepted for transmission
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/src/openhop_core/companion/frame_server/transport.py
+++ b/src/openhop_core/companion/frame_server/transport.py
@@
-    def _enqueue_frame(self, data: bytes) -> None:
+    def _enqueue_frame(self, data: bytes) -> bool:
         if self._write_queue is None:
-            return
+            return False
@@
             self._write_queue.put_nowait(frame)
+            return True
         except asyncio.QueueFull:
             logger.warning(...)
+            return False
--- a/src/openhop_core/companion/frame_server/commands_messaging.py
+++ b/src/openhop_core/companion/frame_server/commands_messaging.py
@@
-        msg = self.bridge.sync_next_message()
+        reservation = self.bridge.reserve_next_message()
+        msg = reservation.message if reservation else None
@@
-        self._write_frame(self._build_message_frame(msg))
+        if self._write_frame(self._build_message_frame(msg)):
+            reservation.commit()
+        else:
+            reservation.release()
