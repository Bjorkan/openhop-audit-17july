# BUG-011: GPS clock corrections can reset rolling airtime and rate-limit windows
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

--- a/repeater/airtime.py
+++ b/repeater/airtime.py
@@
-        now = time.time()
+        now = time.monotonic()
@@
-        self.tx_history.append((time.time(), airtime_ms))
+        self.tx_history.append((time.monotonic(), airtime_ms))
--- a/repeater/handler_helpers/advert.py
+++ b/repeater/handler_helpers/advert.py
@@
-        now = time.time()
+        now = time.monotonic()  # for token buckets, dedupe, hysteresis and penalties
