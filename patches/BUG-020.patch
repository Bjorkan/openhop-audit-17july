# BUG-020: An update install can overlap a version check and be reported idle while still running
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/repeater/web/update_endpoints.py
+++ b/repeater/web/update_endpoints.py
@@
     def start_install(self, thread: threading.Thread) -> bool:
         with self._lock:
-            if self.state == "installing":
+            if self.state not in ("idle", "complete", "error"):
                 return False
+            self._operation_id += 1
+            operation_id = self._operation_id
             self.state = "installing"
@@
-    def _finish_check(self, latest: str) -> None:
+    def _finish_check(self, operation_id: int, latest: str) -> None:
         with self._lock:
+            if self.state != "checking" or operation_id != self._operation_id:
+                return
