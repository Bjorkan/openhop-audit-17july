# BUG-024: An enhanced raw callback is invoked twice when its handler raises
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/src/openhop_core/node/dispatcher.py
+++ b/src/openhop_core/node/dispatcher.py
@@
     async def _invoke_enhanced_raw_callback(...):
-        try:
-            await_or_call(callback, pkt, data, analysis)
-        except Exception:
-            await_or_call(callback, pkt, data)
+        args = (pkt, data, analysis)
+        try:
+            inspect.signature(callback).bind(*args)
+        except (TypeError, ValueError):
+            args = (pkt, data)
+        try:
+            await self._call_callback(callback, *args)
+        except Exception as exc:
+            self._log(f"Raw callback error: {exc}")
