# BUG-013: Failed companion deliveries are recorded as successfully delivered
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/repeater/packet_router.py
+++ b/repeater/packet_router.py
@@
-                consumed = await self._fan_out_to_bridges(... ) or consumed
-                self._mark_delivered_to_companions(packet)
+                delivered = await self._fan_out_to_bridges(...)
+                consumed = delivered or consumed
+                if delivered:
+                    self._mark_delivered_to_companions(packet)
@@
-                await self._fan_out_to_bridges(packet, companion_bridges, context="RESPONSE")
-                self._mark_delivered_to_companions(packet)
+                delivered = await self._fan_out_to_bridges(
+                    packet, companion_bridges, context="RESPONSE"
+                )
+                if delivered:
+                    self._mark_delivered_to_companions(packet)
