# BUG-027: Concurrent message persistence can remove a different, newer message from memory
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/src/openhop_core/companion/message_queue.py
+++ b/src/openhop_core/companion/message_queue.py
@@
+    def remove_identity(self, msg: QueuedMessage) -> bool:
+        for index, queued in enumerate(self._queue):
+            if queued is msg:
+                del self._queue[index]
+                return True
+        return False
--- a/repeater/companion/frame_server.py
+++ b/repeater/companion/frame_server.py
@@
-    async def _persist_companion_message(self, msg_dict: dict) -> None:
+    async def _persist_companion_message(self, msg_dict: dict, queued_message) -> None:
@@
-            self.bridge.message_queue.pop_last()
+            self.bridge.message_queue.remove_identity(queued_message)
