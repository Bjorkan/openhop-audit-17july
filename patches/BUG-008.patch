# BUG-008: Configuration export cannot be fully restored by configuration import
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

--- a/repeater/web/api_endpoints.py
+++ b/repeater/web/api_endpoints.py
@@
                 "logging",
                 "radio_type",
+                "duty_cycle",
+                "gps",
+                "http",
+                "policy",
+                "sensors",
+                "storage",
             }
@@
-                    logger.info(f"Config import: skipping unknown section '{section}'")
-                    continue
+                    skipped_sections.append({"section": section, "reason": "unsupported"})
+                    continue
