# POSSIBLE-ENHANCEMENT-017: Possible enhancement — add bounded backpressure and queue metrics to the storage writer
# Audit date: 2026-07-17
# Classification: Possible enhancement
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/repeater/data_acquisition/storage_collector.py
+++ b/repeater/data_acquisition/storage_collector.py
@@
-        self._db_executor = ThreadPoolExecutor(max_workers=1, ...)
+        self._writer_queue = queue.Queue(maxsize=configured_max_pending)
+        self._writer_thread = threading.Thread(target=self._writer_loop, ...)
+        self.writer_rejected = 0
