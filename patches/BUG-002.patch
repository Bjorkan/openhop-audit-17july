# BUG-002: Configuration screens misread the standard API response envelope
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

# Frontend source files were not included in the supplied snapshot. Regenerate the bundle after applying a source-level helper similar to:

const body = response.data ?? {};
const payload = body.data ?? body;
if (body.success === true) {
  showSuccess(payload.message ?? "Settings saved successfully");
} else {
  showError(body.error ?? payload.error ?? "Failed to save settings");
}
