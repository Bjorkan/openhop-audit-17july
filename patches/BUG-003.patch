# BUG-003: Live duty-cycle limit changes do not update the enforced limit
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

--- a/repeater/airtime.py
+++ b/repeater/airtime.py
@@
 class AirtimeManager:
@@
+    def reload_config(self) -> None:
+        duty = self.config.get("duty_cycle", {})
+        self.max_airtime_per_minute = int(duty.get("max_airtime_per_minute", 3600))
+
--- a/repeater/config_manager.py
+++ b/repeater/config_manager.py
@@
+            if "duty_cycle" in sections:
+                airtime = getattr(getattr(self.daemon, "repeater_handler", None), "airtime_mgr", None)
+                if airtime is None or not hasattr(airtime, "reload_config"):
+                    live_update_ok = False
+                else:
+                    airtime.reload_config()
