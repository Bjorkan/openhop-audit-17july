# BUG-009: Configuration import reports success even when persistence fails
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This file may be incomplete where only compiled frontend assets were supplied.

--- a/repeater/web/api_endpoints.py
+++ b/repeater/web/api_endpoints.py
@@
-            self.config_manager.update_and_save(
+            result = self.config_manager.update_and_save(
                 updates={}, live_update=True, live_update_sections=updated_sections,
             )
-
-            saved = self.config_manager.save_to_file()
+            if not result.get("saved", False):
+                return self._error(result.get("error", "Failed to persist imported configuration"))
@@
-                "saved": saved,
+                "saved": True,
+                "live_updated": result.get("live_updated", False),
