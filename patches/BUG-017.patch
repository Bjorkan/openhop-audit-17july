# BUG-017: Live reload can silently reduce the packet deduplication window from one hour to one minute
# Audit date: 2026-07-17
# Classification: Confirmed defect
#
# LLM-GENERATED IMPLEMENTATION SKETCH — REVIEW AND TEST BEFORE USE.
# This patch communicates the intended change surface and may require adaptation.

--- a/repeater/engine.py
+++ b/repeater/engine.py
@@
+def _cache_ttl_from_config(config: dict) -> int:
+    return max(300, int(config.get("repeater", {}).get("cache_ttl", 3600)))
@@
-        self.cache_ttl = max(300, config.get("repeater", {}).get("cache_ttl", 3600))
+        self.cache_ttl = _cache_ttl_from_config(config)
@@
-            self.cache_ttl = repeater_config.get("cache_ttl", 60)
+            self.cache_ttl = _cache_ttl_from_config(self.config)
